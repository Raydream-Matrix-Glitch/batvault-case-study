import OriginsHero from "../components/origins/OriginsHero";

/**
 * Root/Landing route that shows the Origins hero page
 * (no props yet; keeps it dead-simple)
 */
export default function OriginsRoute() {
  return <OriginsHero />;
}
