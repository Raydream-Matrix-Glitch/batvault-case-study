"""
Memory API internal modules.

This package holds auxiliary components for the Memory API such as the
embeddings client.  It is separate from the public ``memory_api``
module to avoid polluting the FastAPI application namespace.
"""