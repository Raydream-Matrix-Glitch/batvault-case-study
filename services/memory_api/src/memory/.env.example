# Memory API embeddings configuration

# Enable or disable vector embeddings.  When set to true the Memory API
# will call the TEI service to obtain query embeddings and refine BM25
# results.  Set to false to disable and always fall back to BM25-only mode.
ENABLE_EMBEDDINGS=true

# Base URL of the TEI embeddings service.  Do not include the trailing slash.
EMBEDDINGS_ENDPOINT=http://tei-embed:8085

# Expected dimensionality of embedding vectors.  Must match the model
# configured on the TEI service (e.g. 768 for bge-base-en-v1.5).
EMBEDDINGS_DIMS=768

